[project]
name = "norn-forecast"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["norn-core", "norn-integration", "numpy>=1.26", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/norn_forecast"]
