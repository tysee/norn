"""
packages/core/src/norn_core/__init__.py

Пакет ядра платформы norn: общие, не зависящие от конкретного воркера примитивы,
которые переиспользуют все сервисы платформы. Здесь живут типизированный
config-слой (YAML + env), фабрика клиента ClickHouse и контракты данных
(forecast-job / forecast-point), задающие единый формат обмена между
forecast-воркером, агентом и integration-слоем.
"""
__version__ = "0.0.0"
